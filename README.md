# boilerplate-site
a simple html boilerplate with a short project already built. Fully encoded and simple
!RECOMENDED!
* use Notepad or notepad++ to edit the file
* the images might not work-put the popcon.png in the file location set in HTML
